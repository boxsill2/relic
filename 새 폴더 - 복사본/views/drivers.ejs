<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <title>F1 Drivers</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/base.css?v=f1" />
  <link rel="stylesheet" href="/css/header-style.css?v=f1" />
  <link rel="stylesheet" href="/css/driver-style.css?v=f1" />
</head>
<body class="theme-f1 drivers-page">
  <%- include('partials/header', { currentPage: 'drivers' }) %>

  <main class="container">
    <section class="page-header">
      <h1 class="page-title">F1 Drivers</h1>
      <a class="btn" href="/drivers?refresh=1">데이터 새로고침</a>
    </section>

    <% if (locals.error) { %>
      <p class="page-desc" style="opacity:.85"><%= locals.error %></p>
    <% } %>

    <section class="grid grid--wide">
      <% (drivers || []).forEach(function(d){ %>
        <a class="driver-card" href="/drivers/<%= d.slug %>" aria-label="<%= d.full_name %> 상세">
          <span class="driver-photo <%= d.photo_src ? '' : 'fallback' %>">
            <% if (d.photo_src) { %>
              <img
                src="<%= d.photo_src %>"
                alt="<%= d.full_name %>"
                loading="lazy"
                decoding="async"
                onerror="this.closest('.driver-photo').classList.add('fallback'); this.remove();"
              />
            <% } else { %>
              <span class="avatar-fallback"><%= d.initials %></span>
            <% } %>
          </span>

          <span class="driver-meta">
            <span class="top">
              <% if (d.number) { %><span>#<%= d.number %></span><% } %>
              <% if (d.code)   { %><span><%= d.code %></span><% } %>
            </span>
            <h3 class="name"><%= d.full_name %></h3>
            <span class="team-line">
              <% if (d.team_logo) { %>
                <img class="team-logo" src="<%= d.team_logo %>" alt="<%= d.team_name %> 로고"
                     onerror="this.style.display='none'">
              <% } %>
              <span><%= d.team_name || '팀 정보 없음' %></span>
            </span>
          </span>
        </a>
      <% }) %>

      <% if (!drivers || drivers.length === 0) { %>
        <p class="page-desc">드라이버 데이터가 비어 있습니다. 우측 상단의 새로고침을 눌러보세요.</p>
      <% } %>
    </section>
  </main>
</body>
</html>
